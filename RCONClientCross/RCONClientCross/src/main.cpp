#include <iostream>
#include "rcon.h"
#include "protocol.h"
#include "cxxopts.hpp"

int main(int* argc, char* argv[])
{
	// Config file: -c <file_path>
	// -s <ip> server ip
	// -p <port> server port (optional)
	// -k <password> server password (optional)
	// -h <show help>
	// -q execute a command and quit (optional)
}